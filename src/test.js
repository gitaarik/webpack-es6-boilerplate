// Node.js default assert library:
// https://nodejs.org/api/assert.html
import assert from 'assert'


describe('Test suite', () => {

	it('should pass', () => {
		assert(true)
		assert.equal(1, 1)
	})

})
